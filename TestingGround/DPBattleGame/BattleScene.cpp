#include "BattleScene.h"

ConsoleWindow BattleScene::window_draw_game(ConsoleWindow window, int windowWidth, int windowHeight)
{
	return window;
}

ConsoleWindow TempWndw::window_draw_game(ConsoleWindow window, int windowWidth, int windowHeight)
{
	return window;
}
